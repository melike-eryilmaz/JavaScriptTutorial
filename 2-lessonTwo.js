/**
 *Değişkenler
	Javascript de değişkenler var keyword ile tanımlanır. (variable-var)
	Değişken adı rakam olamaz,değiken adı rakamla başlayamaz ama rakamla bitebilir.
	_ değişken tanımlarında kullanılabilri
	$ değiken tanımlarında kullanılabilir.
	Fonksiyon içerisinde tanımlanan bir değişken sadece fonksiyon içerisinde kalır.
	Fonksiyon içerisinde var ile değişken tanımlamamız gereklidir.Tanımlamazsak global olarak algılanır.
	
	
 */

 isim = "Melike";

 function getName(){
     var isim = "Merve";
    console.log(isim)
 }
